# pyproject.toml

[build-system]
requires      = ["setuptools>=62.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ftpr"
version = "1.0.0"
dependencies = [
    "statsbombpy>=1.13.0",
    "tqdm>=4.66.4",
    "pandas>=2.2.2"
]
requires-python = ">=3.9"
